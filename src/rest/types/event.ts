export default event;

// TODO: Same as characterEvent?????
type event = {
    character_id: string,
    achievement_id: string,
    timestamp: string,
    zone_id: string,
    world_id: string,
    event_type: string,
    table_type: string,
};
