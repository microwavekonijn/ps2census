export type characterAchievement = {
    character_id: string,
    achievement_id: string,
    start: string,
    start_date: string,
    finish: string,
    finish_date: string,
    last_save: string,
    last_save_date: string,
    [prop: string]: any,
};
